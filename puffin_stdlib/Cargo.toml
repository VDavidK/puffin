[package]
name = "puffin_stdlib"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[features]
default = ["vendor"]
vendor = []

[dependencies]
puffin_runtime = { path = "../puffin_runtime" }
